'use strict'

module.exports = Object.freeze({
	auto: Object.freeze({
		save: 'auto-save'
	}),
	task: Object.freeze({
		new: 'task-new',
		span: Object.freeze({
			new: 'task-span-new',
			update: 'task-span-update'
		}),
		view: 'task-view'
	}),
	log: 'log'
})
